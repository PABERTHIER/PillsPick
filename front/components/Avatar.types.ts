export interface D {
  showActions: boolean
}

export interface M {
  togglePopup: () => void
  connect: () => void
  disconnect: () => void
}

export interface C {
  status: string
}

export interface P {
  isConnected: boolean
}
